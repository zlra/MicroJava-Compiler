program ProveraSintaksnihSmena
	const int x=1;
	int y[], j=;
	bool z;
	
{
	
	int k (int a[], int b) int c; {
		c[3] = 0; // c nije niz
		do {
			break;
			continue;
			a[1] = a + b - c * d / g % e; // a je niz, nekompatibilni tipovi
			k(a, b+3);
			a[5] = k(a, b+3) + 8;
		} while (a[i] < b); // i nije deklarisano
		return c[2];
	}
	
	int main() {
		if (5>3)
			print(1);
		else 
			if (ss != a)
				a++;
		k[3]();
		print(2);
		return;
	}
}